#monitor=,2560x1440@144,auto,1.25
monitor=,3440x1440@143.97,auto,1.25

$mainMod = SUPER
$terminal = alacritty
$fileManager = thunar 
$drun = tofi-drun --drun-launch=true
$menu = wofi --show drun

#env = XCURSOR_THEME,Bibata-Modern-Classic
env = XCURSOR_SIZE,24
env = QT_QPA_PLATFORMTHEME,qt5ct # change to qt6ct if you have that

exec-once = /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1
exec-once = sleep 1 && hyprpm reload
exec-once = swayidle -w timeout 600 '/home/davi/.local/scripts/custom-lock.sh' timeout 700 'wayout --off DP-1' resume 'wayout --on DP-1'
exec-once = /usr/bin/pypr
exec-once = /usr/bin/easyeffects --gapplication-service
exec-once = gammastep -l -22.5:-47.4 -b 1:0.8
exec-once = sleep 2; firewall-applet
exec-once = nm-applet
exec-once = waybar
exec-once = syncthing serve

#set wallpaper
$wallpapersPath = $HOME/Imagens/character-bgs
exec-once = ~/.local/scripts/change-bg.sh first
bind = $mainMod, b, execr, ~/.local/scripts/change-bg.sh n
bind = $mainMod SHIFT, b, execr, ~/.local/scripts/change-bg.sh p
#exec-once = swww img `find $wallpapersPath -type f | shuf -n 1` --transition-type none
#bind = $mainMod SHIFT,B,exec, swww img `find $wallpapersPath -type f | shuf -n 1` --resize crop --transition-type grow --transition-step 240 --transition-duration 1 --transition-fps 80

exec-once = flatpak run com.spotify.Client
exec-once = flatpak run dev.vencord.Vesktop
exec-once = thorium-browser-avx2 --enable-features=UseOzonePlatform --ozone-platform=wayland
exec-once = codium --enable-features=UseOzonePlatform,WaylandWindowDecorations --ozone-platform=wayland --unity-launch

exec-once = swayosd-server
bindl = ,XF86AudioPlay, execr, playerctl --player=spotify play-pause
bindl = ,XF86AudioNext, execr, playerctl --player=spotify next
bindl = ,XF86AudioPrev, execr, playerctl --player=spotify previous
binde = ,XF86AudioRaiseVolume, execr, swayosd-client --output-volume +5
binde = ,XF86AudioLowerVolume, execr, swayosd-client --output-volume -5
bindl = ,XF86AudioMute, execr, swayosd-client --output-volume mute-toggle
bind = ,Print, execr, grim -l 1 -g "$(slurp)" - | swappy -f -

#supress maximize
windowrulev2 = suppressevent maximize, class:.* # You'll probably like this.
windowrulev2 = idleinhibit fullscreen, class:.* # You'll probably like this.

#float

windowrulev2 = float,class:(org.gnome.Loupe)
#windowrulev2 = size 30% 80%,class:(org.pwmt.zathura)
windowrulev2 = size 20% 40%,class:(org.gnome.TextEditor)
windowrulev2 = float,class:(org.gnome.TextEditor)
windowrulev2 = pseudo,class:(evince)
windowrulev2 = pseudo,class:(org.pwmt.zathura)
#windowrulev2 = float,class:(org.pwmt.zathura)
#windowrulev2 = center,class:(org.pwmt.zathura)
windowrulev2 = float,class:(org.gnome.NautilusPreviewer)
windowrulev2 = size 30% 80%,class:(org.gnome.NautilusPreviewer)
windowrulev2 = move 30% 10%,class:(org.gnome.NautilusPreviewer)
windowrulev2 = float,class:(org.gnome.Nautilus)
windowrulev2 = size 45% 65%,class:(org.gnome.Nautilus)
windowrulev2 = float,class:(thunar)
windowrulev2 = float,class:(org.gnome.FileRoller)
windowrulev2 = float,class:(org.gnome.Calculator)
windowrulev2 = float, class:(easyeffects)
windowrulev2 = size 40% 40%, class:(easyeffects)
windowrulev2 = float, class:(obsidian)
windowrulev2 = size 60% 70%, class:(obsidian)
windowrulev2 = float, class:(xdg-desktop-portal-gtk)
windowrulev2 = float, class:(com.github.tchx84.Flatseal)
windowrulev2 = float, class:(pavucontrol)
#windowrulev2 = size 20% 30%, class:(pavucontrol)
#windowrulev2 = move 79.2% 5%, class:(pavucontrol)

#custom rules
windowrulev2 = workspace 1, title:(Thorium)
windowrulev2 = workspace 1, class:(Thorium)
windowrulev2 = workspace 2, class:(codium-url-handler)
windowrulev2 = opacity 0.96, class:(codium-url-handler)
windowrulev2 = noblur, class:(codium-url-handler)
windowrulev2 = opacity 0.96, class:(Alacritty)
windowrulev2 = noblur, class:(Alacritty)

workspace = special:media,gapsout:120
windowrulev2 = workspace special:media silent, class:(discord)
windowrulev2 = group, class:(discord)
windowrulev2 = noinitialfocus, class:(discord)
windowrulev2 = workspace special:media silent, class:(vesktop)
windowrulev2 = group, class:(vesktop)
windowrulev2 = noinitialfocus, class:(vesktop)

windowrulev2 = workspace special:media silent, class:(Spotify)
windowrulev2 = group, class:(Spotify)
windowrulev2 = noinitialfocus, class:(Spotify)

#workspace = special:pdf,gapsout:180
windowrulev2 = workspace special:pdf,class:(evince)
windowrulev2 = group,class:(evince)
windowrulev2 = workspace special:pdf,class:(org.pwmt.zathura)
windowrulev2 = group,class:(org.pwmt.zathura)

#always open terminal
#windowrulev2 = workspace special:alacritty silent, class:(dropin)
windowrulev2 = float, class:(dropin)
#windowrulev2 = size 70% 40%, class:(dropin)
#windowrulev2 = move 15% 5%, class:(dropin)

#bind = $mainMod, P, exec, ~/.local/scripts/new-default-terminal.sh
#bind = $mainMod SHIFT, P, movetoworkspacesilent, special:alacritty 
bind = $mainMod, P, execr, ~/.local/scripts/dropdown-terminal.sh 

bind = $mainMod, Comma, togglespecialworkspace, pdf 
bind = $mainMod SHIFT, Comma, movetoworkspace, special:pdf 
bind = $mainMod, M, togglespecialworkspace, media 
bind = $mainMod, W, execr, pkill -SIGUSR1 waybar
bind = $mainMod ALT, R, execr, ~/.local/scripts/change-resolution.sh 
# Source a file (multi-file configs)
# source = ~/.config/hypr/myColors.conf

# For all categories, see https://wiki.hyprland.org/Configuring/Variables/
group{
    col.border_active = rgb(7DAEA3)
    col.border_locked_active = rgb(7DAEA3)
    groupbar {
        col.active = rgb(7DAEA3)
        col.inactive = rgb(2E3B3B)
        enabled=true
        render_titles=false
        height=4
    }
}

misc{
    disable_hyprland_logo = true
    disable_splash_rendering = true
}

input {
    kb_layout = br
    sensitivity = 0 # -1.0 to 1.0, 0 means no modification.
    mouse_refocus = false
    float_switch_override_focus = 1
    follow_mouse = 1
}

general {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more

    no_cursor_warps = true
    gaps_in = 10
    #gaps_out = 15
    border_size = 4
    col.active_border = rgba(A9B665FF) rgba(89B482FF) 30deg 
    col.inactive_border = rgba(1D2021ff)

    layout = dwindle

    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = false
}

decoration {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more

    rounding = 10
    
    blur {
        enabled = true
        size = 3
        passes = 1
    }

    drop_shadow = yes
    shadow_range = 4
    shadow_render_power = 3
    col.shadow = rgba(1a1a1aee)
    #layerrule = blur,waybar
}

animations {
    enabled = true

    bezier = myBezier, 0.05, 0.9, 0.1, 1.05

    animation = windows, 1, 7, myBezier
    animation = windowsOut, 1, 7, default, popin 80%
    animation = border, 1, 10, default
    animation = borderangle, 1, 8, default
    animation = fade, 1, 7, default
    animation = workspaces, 1, 6, default
}

dwindle {
    # See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
    pseudotile = true # master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # you probably want this
    force_split = 2
    smart_resizing = false
}

master {
    # See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
    new_is_master = true
}

gestures {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more
    workspace_swipe = off
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#per-device-input-configs for more
device {
    name = epic-mouse-v1
    sensitivity = -0.5
}

xwayland {
    force_zero_scaling = true
}

#suprimindo evento de maximização das janelas

# See https://wiki.hyprland.org/Configuring/Keywords/ for more

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = $mainMod, Return, exec, $terminal
bind = $mainMod, Q, killactive, 
bind = $mainMod SHIFT, E, execr, nwg-bar -i 80
bind = $mainMod, E, exec, $fileManager
bind = $mainMod SHIFT, Space, togglefloating, 
bind = $mainMod, D, execr, pkill tofi || $drun
bind = $mainMod, C, execr, hyprpicker -a
bind = $mainMod, Y, fullscreen, 0
bind = $mainMod, Y, toggleopaque
bind = $mainMod SHIFT, Y, fullscreen, 1
#bind = $mainMod, J, togglesplit, # dwindle

# Move focus with mainMod + arrow keys
#bind = $mainMod, H, exec, ~/.local/scripts/focus.sh left 
bind = $mainMod, A, pseudo
binde = $mainMod, L, execr, ~/.local/scripts/focus.sh next r
binde = $mainMod, H, execr, ~/.local/scripts/focus.sh prev l
binde = $mainMod, K, execr, ~/.local/scripts/focus.sh next u
binde = $mainMod, J, execr, ~/.local/scripts/focus.sh prev d

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, U, execr, ~/.local/scripts/change-workspace.sh 1
bind = $mainMod, I, execr, ~/.local/scripts/change-workspace.sh 2
bind = $mainMod, O, execr, ~/.local/scripts/change-workspace.sh 3
bind = $mainMod, 1, execr, ~/.local/scripts/change-workspace.sh 1
bind = $mainMod, 2, execr, ~/.local/scripts/change-workspace.sh 2
bind = $mainMod, 3, execr, ~/.local/scripts/change-workspace.sh 3
bind = $mainMod, 4, execr, ~/.local/scripts/change-workspace.sh 4
bind = $mainMod, 5, execr, ~/.local/scripts/change-workspace.sh 5
bind = $mainMod, 6, execr, ~/.local/scripts/change-workspace.sh 6
bind = $mainMod, 7, execr, ~/.local/scripts/change-workspace.sh 7
bind = $mainMod, 8, execr, ~/.local/scripts/change-workspace.sh 8
bind = $mainMod, 9, execr, ~/.local/scripts/change-workspace.sh 9
bind = $mainMod, 0, execr, ~/.local/scripts/change-workspace.sh 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, U, movetoworkspace, 1
bind = $mainMod SHIFT, I, movetoworkspace, 2
bind = $mainMod SHIFT, O, movetoworkspace, 3
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10


# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow
